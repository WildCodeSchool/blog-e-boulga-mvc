{% extends 'layout.html.twig' %}

{% block content %}
    {{ dump(article) }}
    <main>
        <section class="content-width body_text">
            <h1 id="article_title">
                {{ article.title }}
            </h1>

            <p class="content-width article_P">
                {{ article.description }}
            </p>
        </section>
        <section id="img_section">
            <img id="article_img" src="assets/images/article/{{ article.img_src
            }}" alt="<?php echo
            $altImg === " ? "Image de l'article " . $title : $altImg; ?>" />
        </section>

        <section class="content-width body_text">
            <h2 class="body_title">Introduction</h2>
            <p class="article_P">
                {{ article.img_src }}{{ article.introduction }}
            </p>

            <h2 class="body_title">Le détail</h2>
            <p class="article_P">
                {{ article.detail }}
                {{ classname }}
            </p>
        </section>
    </main>
{% endblock %}
